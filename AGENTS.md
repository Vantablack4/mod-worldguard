# Vantablack Mind Bootstrap

Project: mod-worldguard

At the start of Vantablack mod work, load memory automatically:

```bash
node /Users/denizsonmez/Projects/vantablack/Vantablack4/scripts/vb-mind.mjs context --cwd "$PWD"
```

Use that output as orientation, then inspect authoritative files in this repo
before editing or making claims. Do not ask the user to run memory commands
during normal Codex work.
